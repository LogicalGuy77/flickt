/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com" , 'plus.unsplash.com' , 'images.unsplash.com'], // Allow external images from this domain
  },
};

export default nextConfig;
